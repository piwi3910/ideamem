import { PreConfiguredRoutes } from '@/lib/handlers/crud-factory';

// Export the pre-configured CRUD routes for global rules
export const { GET, POST, PUT, DELETE } = PreConfiguredRoutes.globalRules;
