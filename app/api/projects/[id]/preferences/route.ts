import { PreConfiguredRoutes } from '@/lib/handlers/crud-factory';

// Export the pre-configured CRUD routes for project preferences
export const { GET, POST, PUT, DELETE } = PreConfiguredRoutes.projectPreferences;

